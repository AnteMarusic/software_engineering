package org.example;

public class Controller {

    private Fase currentFase;
    private Player[] orderedPlayer;

    private Player nextPlayer() {
        currentPlayer = currentFase.getPlayer();

    }

    public void run() {
        while () {

        }
    }
    public void selectCard(coordinates c1, c2, c3 ) {
        //insieme di if che controllano se le coordinate sono allineate
        if(Board.getCoordinate(c1) == null) {
            throws Exception
        }
        buffer.insert(card1, card2, card3);
        Board.remove();
        view.updateBoard();
    }

    public void sortBuffer() {

    }


    public void insertInBookshelf() {

    }
}
