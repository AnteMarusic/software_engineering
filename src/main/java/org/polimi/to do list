TODO
- riconnessione del client disconnesso
- se chiudi il server e mandi un messaggio da un socket client connesso in una partita esce un'eccezione "socket closed"
-togliere print da RMIclient e SocketClient
-print della CLI accazzo a causa del multithreading di RMI non ci saranno nella versione finale (sono cose di debug)
-se finisce la partita manca la parte in cui si tolgono gli username dei player da username issuer
- CLI: stampare la bookshelf personale accanto alla board e le bookshelf degli altri una accanto all'altra
       migliorare l'aspetto dell'array di carte scelte
- il codice delle partite private va scelto dal server
- se un client scrive merda quando non è il suo turno poi quando arriva il suo turno tutto lo schifo che ha scritto rimane
        nell'input e quindi vengono stampati mille volte i messaggi di errore di input
- migliorare la leggibilità del processo di scelta delle carte
- quando uno sceglie delle carte, siccome non viene cambiate la board ma solo l'array di carte scelte forse conviene stampare solo quello
- mettere un thread sempre attivo sul client che se non sono in una fase in cui devo scrivere allora qualsiasi cosa scrivo mi dice
    che non posso scrivere e di aspettare il mio turno (e poi fa flush dello stream di input)
- i file vengono bufferizzati. c'è un modo per togliere la bufferizzazione e vedere subito i cambiamenti sul file
- rendere più carini i buffer di riordino delle carte della CLI
- se un socketClient si disconnette (ad esermpio viene chiuso il server) mentre sta scegliendo le carte esce un'eccezione
        soluzione: mettere una variabile "disconnesso ai clientHandler per evitare che la disconnessione venga fatta due volte"
-RMI si accorge correttamente se il server si disconnette, socket no
- non mi ha fatto fare undo alla seconda scelta della carta sulla cli
- verifica che con la riconessione del primo giocatore di una partita che ricomincia viene assegnato il giocatore corrente

- in una partita da 3 giocatori gli shared score sono 6,4,2 dovrebbero essere 8,6,4
- se nella mia bookshlef ho al massimo uno spazio per 2 caselle il gioco me ne fa prendere anche 3
